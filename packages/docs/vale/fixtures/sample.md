# Calibration sample — one case per rule class, plus pinned negatives.
# Lines that must NOT alert are labeled; everything else is a plant.

The "Tables empty after drain" guarantee can fail to hold even on a host that ran release N for days. It does NOT provably beat the transfer sweep, and the refusal does **not** assume the host "skipped" release N. The prior attempts measured a channel now known to have been dead, not the work. The clamp is a floor, not a ceiling — a caller that wants a longer live window keeps it — and a kind with no declared TTL (dispatch wakeups, task firings) keeps the caller's value verbatim. Deleting a feed cancels its linked event task. Its delivery thread remains. The file tools guard the system database (#207). On Windows, the CI oracle proves allowed-root writes. The MSI outage of 2026-08-31 proved these rows can stay pending for days. Redrive after a transient outage has ended; the runtime image check runs in release CI. This genuinely local recovery path is not leader-gated. The *transfer-reclaim* leg uses italics here. It’s “quoted”, with an em-dash — and ellipsed… You should restart your host. A feed pointed at a blocked address fails its poll with a "not publicly routable" error and delivers nothing. Again, the backend is illustrative and does not determine hub or spoke. Importing a skill adds it to the catalog; it does not activate the skill. Importing a skill makes it available for activation, but does not activate it in any thread. Deletion soft-deletes the catalog artifact and its files. It does not make claims about context. The whole table is host-local (the sentinel only distinguishes wakeups from transfer rows, it does not mark rows), so peer-targeted dead letters — result, inference, cancel — are cleared too, which is the backlog this command exists for. Deleting rows is destructive (see the note). **A pending peer-targeted row is the durable spool-transfer queue, not residue.**

- **List lead**: structural bold; BoldSentence must not fire.
- The **terminal-expiry** leg names a defined term mid-sentence.

1. **What did the turn request?** This bolded numbered lead-in must not alert.
2. **Which backends are eligible?** Neither should this one.

ASCII diagram fragment follows:

↓
Passive and existential plants: the frame is queued into the socket buffer. Rows are cleared too. The row is classified by the receiver. The refusal does not assume the host skipped release N. There are two selectors. There is a subtler failure.
Colon-density paragraph: this paragraph leans on the colon. Second lead-in follows: another elaboration. Third: still counting. Fourth colon: this trips the density rule.
